import { FechaYhoraPipe } from './fecha-yhora.pipe';

describe('FechaYhoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaYhoraPipe();
    expect(pipe).toBeTruthy();
  });
});
